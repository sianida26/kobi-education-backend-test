const path = require('path');
// require('dotenv').config();

const MODELS_DIR = path.join(__dirname, 'src/database/models');
const CONFIG_DIR = path.join(__dirname, 'src/database');
const MIGRATIONS_DIR = path.join(__dirname, 'src/database/migrations');
const SEEDERS_DIR = path.join(__dirname, 'src/database/seeders');

module.exports = {
  config: path.join(CONFIG_DIR, 'config.ts'),
  'models-path': path.join(MODELS_DIR),
  'migrations-path': path.join(MIGRATIONS_DIR),
  'seeders-path': path.join(SEEDERS_DIR),
};